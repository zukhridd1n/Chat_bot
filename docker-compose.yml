version: "3.9"

services:
  bot:
    build: .
    container_name: chat-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./backup:/app/backup
      - ./media:/app/media
